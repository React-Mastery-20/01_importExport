// Important Points:

    // No curly braces.

    // Import name anything, no strict name-matching needed.

    // Only one default export allowed per file.

import greet from "./02default_export.js"

greet();